# 3D渲染的探险部分

## 使用第三方库

+ 基于OpenGL
+ glfw
+ glm
+ glad
+ tiny_obj_loader
+ stb_image

## 注意事项

+ 这个分支尚未完工！！！
    因为整体架构还未确定的原因，本分支目录不代表最终架构设计。故现在只是将大版本更新之后的文件复制到这里，最后在整体架构确定之后会在本目录下进行开发。
    故commit记录会不准确。

+ 本模块原理远没有大家想象中的复杂，但是其中包含非常多的面向对象设计模式，非常欢迎大家阅读修改。

## 已经实现

+ 定点着色器和片段着色器加载
+ 立方体和球体的创建
+ 键盘和鼠标的总体控制
+ 基础phone光照渲染
+ obj文件导入
+ 纹理贴图支持

## 尚未实现

+ 网格化地图 封装棋盘
+ 便捷的cube_list管理
+ 拓展几何体
+ 多光源设计
+ imgui重叠ui
+ 更酷炫的obj和shader
+ logger 功能，是不是可以分是在debug 和info模式，不是cout的形式而是日志保存
+ cube 等集合体的拓展，强制硬编码可以显著减少游戏加载时常（包括多面上色纹理，
+ 氛围渲染和粒子效果
+ PBR光照
+ 抽象出实体 抽象出camera 抽象出光源？？这里面向对象设计应该是怎么样的？
+ objmodels 可能需要清理，感觉由于一开始的设计导致这部分的实现有地点奇怪，draw感觉划分的太开，不如设定texture和material的初始值
+ loader的架构和集合体model的创建需要放在render文件夹内部吗，是不是应该分开
这里有点挤。类似shader material texture mesh放在一起 剩下其余集合体的实现放在其他地方

## 还没有理解

+ OpenGL 3D渲染逻辑
+ 鼠标回调 窗口管理逻辑
+ mesh factory的意义在哪里
+ model这个类是将一个model所有VAO VBO都集合在一起吗，是怎么做到识别不同纹理分批次分颜色渲染的
+ 查询还有什么很炫酷但是实现起来原理比较简单的功能
+ 着色器可以实现多物体不同着色器风格渲染吗，是不是要进行图层的叠加

## 抛弃功能

处于多种考量，抛弃适当功能，对于3d渲染的项目进行了精简

+ 多item一次传递绘制的优化
+ 多材质一次绘制的优化
+ model大类（集合不同的mesh）的优化
+ 放弃使用Assimp模块进行加载

## 感受

+ 好像是实在的体会到了工程本质上的trade off的感觉
